# 5000路验证与本轮逐项复核

文档集 1.4.0，复核日期 2026-09-06。最终代码在隔离 Linux 虚拟机连续两轮完成 5000 路真实 SIP 与双向 PCMU 媒体验收。

## 最终两轮结果

| 轮次 | 同时接通 | 媒体时长 | 实际发送 / 唯一接收 | 名义负载完成率 | 实际缺包 | 拆线失败 | 清理 |
|---|---:|---:|---:|---:|---:|---:|---|
| final-5000-01 | 5000 | 10 秒 | 4,999,993 / 4,999,993 | 99.999860% | 0 | 0 | 全部完成 |
| final-5000-02 | 5000 | 10 秒 | 4,999,946 / 4,999,946 | 99.998920% | 0 | 0 | 全部完成 |

每轮标称双向总包量 5,000,000；门禁保持原定的逐方向、逐流至少 98% 负载，并要求所有实际发送包被唯一接收。两轮每方向每路最低收到 499 个包，部分方向达到 500；不存在用总吞吐掩盖未发声通话的情况。

两轮日志均证明同一控制实例下 5000 路同时接通；8 个媒体分片均无重启，最终活跃通话归零，媒体 socket 丢弃及应用收发错误为零，事件日志无丢失。进程退出、端口释放和临时目录删除均由测试管理器完成并核对。

### 实际环境和证据边界

宿主为共享使用的 ARM64 Mac mini，10 个逻辑 CPU、16 GiB 内存；测试在 Apple Virtualization.framework 启动的 Alpine 3.24.1 / Linux 6.18.35-virt ARM64 虚拟机中执行，分配 8 vCPU、2 GiB。服务、8 个媒体分片、8 个发生器发送工作者共享该虚拟机，使用隔离回环地址 127.77.0.2、128 组共享端点、512 个端点 UDP socket。100 CPS 建立呼叫，PCMU / 20 ms，完整媒体窗口 10 秒。

测试期间为虚拟机进程持有系统提供的 latencyCritical / userInitiated 活动，结束即释放；用于降低系统节能对实时定时的影响。前期失败与最终成功之间还存在宿主负载、虚拟机内存和发生器版本的变化，不能将差异全部归因于某一代码优化。环境做法见 [Apple 实时活动文档](https://developer.apple.com/documentation/foundation/processinfo/activityoptions/latencycritical)。

| 轮次 | 整机 UDP InErrors / RcvbufErrors 增量 | UDP NoPorts 增量 | 媒体 socket_rx_drops | 实际 RTP 缺包 |
|---|---:|---:|---:|---:|
| final-5000-01 | 126 / 126 | 69 | 0 | 0 |
| final-5000-02 | 204 / 204 | 125 | 0 | 0 |

整机UDP InErrors/RcvbufErrors增量不为零，尚未定位具体流量；媒体分片socket丢弃为零且实际RTP发送等于唯一接收。不能表述为整机UDP零丢包。

本结果不覆盖长时间稳定性、真实网卡/线路、转码、会议、录音或一万路；静态 musl 构建中的动态加载不可用，因此也不证明 Linux 原生 G.722/Opus SDK 转码。未执行原版 FreeSWITCH，本轮没有授予任何成对兼容认证。当前 macOS 管理页面仍执行自身端口预检，5000 路按钮不会自动调度此隔离 Linux 虚拟机。

## 代码修复与回归

- 发生器在 Linux ARM64/AMD64 使用批量收发，按真实 socket 分配固定发送工作者并提前准备包缓冲。保留节奏控制、部分发送只重试未发送后缀及有界信号中断重试。
- 5000 路拆线采用最多 64 个工作者，独立维护每路请求和超时，避免每路一个 goroutine 或串行拆线队列。逐流不足量、未知/额外包及早退读取都计为失败。
- Rust 复用 Linux 批量接收描述符；每批重绑缓冲地址、重置内核输出，保留边沿通知一直排空到 EAGAIN 的要求，避免旧控制信息或截断数据误判。相同时刻准入计算走快路径但不补回已花费令牌。
- XML 元数据启动时解析并有界缓存；输出快照独立持有切片，读写中的纯计算移出管理锁，保存前再次检查配置版本。慢 XML 响应不再拖住状态与排空接口。持久化提交仍包含同步写盘，慢盘场景尚需实际硬件验证。
- 修复账本漏列 21 个动态编码用例；通过有界 AST 枚举列出定义，不执行测试文件。绿色状态单独绑定真实运行、当前源码、构建产物、完整日志和复查期限；缺失证据、过期、源码变化或失败均不会显示绿色。

全量 Go 竞态回归通过 303 个 test/subtest 事件；两种 UDP 模式共 90 项真实 SIP/媒体端到端用例通过。宿主 Rust 25 项库测试和 1 项集成测试通过；Linux 实际执行 27 项 Rust 库测试，批量系统调用布局、真实收发、截断、部分发送、每路足量与有界拆线等发生器测试通过。新开发路径均附中文说明。

## 对照表逐项处理

固定 FreeSWITCH 1.11.3 / `ef32e205295e29f034f1453ad245ba5efb07b94a`。逐项核对全部 3979 个原始 ID，原始实现状态与本地运行状态分别保留。当前 30 项有有效的本地限定范围证据，其余保持待执行或未实现；绿色数量不是原版兼容率。

| 原始实现状态 | 条数 | 处理方式 |
|---|---:|---|
| export_only | 882 | 验证编辑/导出子集，原版XML业务执行仍缺失 |
| implemented | 17 | 自有接口；按具体测试断言授予绿色，不升级为原版线协议兼容 |
| internal_only | 6 | 内部合同与原版外部接口不同，保留边界 |
| not_implemented | 2884 | 没有对应运行处理器，保留缺口和下一步要求 |
| not_verified | 178 | 177项兼容定义与万路目标；5000路单独列有限定的绿色附加状态 |
| partial | 12 | 只按实际覆盖的子集验证，不推导完整合同 |

全部项目的剩余问题与后续要求见 [3979项逐项处理表](verification-5000-entries.csv)、[运行验证](runtime-verification.md)和[完整功能审计](feature-audit.md)。ESL 入站/出站与事件、用户 REGISTER/Digest、完整 Sofia 生命周期、拨号计划/目录执行、会议、录音、脚本应用和原版 C ABI 等仍存在实际功能缺口；仅有模块名、配置文件或测试定义不能验证它们已兼容。

## 历次尝试完整保留

以下为本轮最终构建之前的真实试验。诊断版改变节奏或 profile，只用于定位；其结果没有冒充最终代码证据。宿主负载与环境并不相同，不用这些数字计算受控性能提升百分比。

| 尝试 | 通过 | 发送 / 唯一接收 | 名义负载完成率 |
|---|---|---:|---:|
| baseline-1000-ready | 是 | 999904 / 999904 | 99.990400% |
| baseline-2g-5000 | 否 | 2905658 / 2905658 | 58.113160% |
| baseline-5000 | 否 | 3276162 / 3274812 | 65.523240% |
| diagnostic-no-floor-5000 | 否 | 1046153 / 1046153 | 20.923060% |
| diagnostic-profile4-activity-5000 | 否 | 2261598 / 2260390 | 45.231960% |
| optimized-initial-5000 | 否 | 668217 / 668217 | 13.364340% |
| optimized-v2-2g-5000 | 否 | 346957 / 333747 | 6.939140% |
| optimized-v2-activity-lowerload-5000 | 是 | 4999894 / 4999894 | 99.997880% |

另外保留最初主服务未就绪的采集尝试、虚拟机回环未启用的工具测试失败、一次 XML 慢请求测试失败，以及宿主执行文件被原地覆盖造成媒体子进程退出的 E2E 失败；后续修复后的完整重跑日志另存，未覆盖旧记录。宿主可执行文件以新文件原子替换安装。

## 文档与证据复核

[机器报告](verification-5000-2026-09-06.json)保留最终包量、方向、内核增量、环境、清理及二进制 SHA；[运行验证数据](runtime-verification.json)随文档内嵌有界压缩原始日志，可离线重新判定绿色。完整二进制、状态采样、事件、失败报告与构建记录收录在 `rustswitch-compatibility-verification-v1.4.0.zip`。

运行测试的宿主/Linux 二进制和最终内嵌新版文档的宿主控制程序分别记录 SHA。文档发布不改变已验证的 Go/Rust 功能源码；实际容量证据只引用当时真正运行的 Linux 产物。
