# RustSwitch 1.8 缺口推进与逐项验证

本轮新增通道快照和四类放音／停泊事件，补充既有通道 API 的独立验收，并修正后台计数展示。以下结论来自冻结代码构建后实际执行，完整 FreeSWITCH 兼容、全协议覆盖及容量认证仍未完成。

## 数量变化

| 统计维度 | 1.7 基线 | 1.8 当前 |
|---|---:|---:|
| 全部原始对照 ID | 3985 | 3985 |
| 本地限定验证通过（绿色） | 54 | 66 |
| 本地未执行 | 3930 | 3918 |
| 本地验证失败 | 1 | 1 |
| 未实现兼容入口 | 2851 | 2846 |
| 部分实现入口 | 46 | 51 |
| 完整契约待验收（实现分类） | 178 | 178 |

“完整契约待验收”的178由177份验收定义与容量目标组成，不是未运行测试总数。因此本轮保持178，同时将实际执行进展独立显示。实现维度另有882条配置导出、17条自有接口和11条内部协议；这些数值不代表原版兼容通过。目录包含声明、配置出现位置和验收定义，3985不是3985个独立模块。

页面首屏显示本地通过、未执行、失败和失效证据；展开“本轮进展”可定位新增12个原始ID。所有当前绿色都有实际执行收据、源码绑定和范围，旧版成功不能覆盖新版失败。页面刷新读取最新发布证据，不会自动执行测试。

## 本轮逐项结果

| 原始 ID | 入口／事件 | 当前限定结果 |
|---|---|---|
| `event:CHANNEL_ANSWER` | CHANNEL_ANSWER | 两种UDP模式下逐腿真实创建/接听/挂机、唯一身份、SIP来源、互相关联与时序；限定基本字段，不包含完整原版生命周期 |
| `event:CHANNEL_CREATE` | CHANNEL_CREATE | 两种UDP模式下逐腿真实创建/接听/挂机、唯一身份、SIP来源、互相关联与时序；限定基本字段，不包含完整原版生命周期 |
| `event:CHANNEL_HANGUP` | CHANNEL_HANGUP | 两种UDP模式下逐腿真实创建/接听/挂机、唯一身份、SIP来源、互相关联与时序；限定基本字段，不包含完整原版生命周期 |
| `event:CHANNEL_PARK` | CHANNEL_PARK | 两种UDP模式下真实park进入、通话保持及挂机退出顺序；仅受限park路径，不含原版transfer/unpark全部路径 |
| `event:CHANNEL_UNPARK` | CHANNEL_UNPARK | 两种UDP模式下真实park进入、通话保持及挂机退出顺序；仅受限park路径，不含原版transfer/unpark全部路径 |
| `event:PLAYBACK_START` | PLAYBACK_START | 两种UDP模式真实WAV/单音开始、自然结束/按键/API停音、挂机/worker故障回收与逐腿事件顺序；不含原版全部字段及loading即挂机的未观测窗口 |
| `event:PLAYBACK_STOP` | PLAYBACK_STOP | 两种UDP模式真实WAV/单音开始、自然结束/按键/API停音、挂机/worker故障回收与逐腿事件顺序；不含原版全部字段及loading即挂机的未观测窗口 |
| `registration:api:mod_commands:uuid_dump:7744` | uuid_dump | 两种UDP模式下真实双腿快照、txt/plain/JSON/XML与未知格式回退、中文变量隔离、结束后查无及再次呼叫媒体；不含原版完整字段和状态机 |
| `registration:api:mod_commands:uuid_exists:7745` | uuid_exists | 两种UDP模式下活动/已删除通道、中文变量读写删除、双腿隔离、非法输入与媒体保持；不包含全部原版变量求值与副作用 |
| `registration:api:mod_commands:uuid_getvar:7748` | uuid_getvar | 两种UDP模式下活动/已删除通道、中文变量读写删除、双腿隔离、非法输入与媒体保持；不包含全部原版变量求值与副作用 |
| `registration:api:mod_commands:uuid_kill:7750` | uuid_kill | 两种UDP模式下NORMAL_CLEARING实际双腿BYE、资源回收及再次呼叫；非法原因拒绝且通话保留，不代表全部挂机cause |
| `registration:api:mod_commands:uuid_setvar:7777` | uuid_setvar | 两种UDP模式下活动/已删除通道、中文变量读写删除、双腿隔离、非法输入与媒体保持；不包含全部原版变量求值与副作用 |

新增实现是 uuid_dump 与四个生命周期事件，共5项；其余7项是既有3个通道事件与4个UUID API新增独立验收映射，没有冒充本轮新实现。12项均只授予表内子集通过，不修改完整契约通过标志。

## 具体修复

- `uuid_dump` 在真实通道主循环按UUID定位，支持txt/plain/JSON/XML、未知格式回退、双腿变量隔离、中文名称和值、结束后查无。依据原版实测修正plain方括号和百分号规则；非法XML名称及超预算请求明确失败。
- 放音 START/STOP 依据媒体回执发布；文件缺失无伪开始，按键和API打断确认停音后完成，挂机与worker故障等待媒体释放。乱序回执不会造成完成后迟到开始。
- park 实际进入／离开执行槽时发 PARK/UNPARK，并保持每腿身份和完成顺序。
- 媒体故障重试采用100/200/400/800/1000毫秒上限退避和0–36毫秒错峰；每腿单个在途RPC、每轮最多256项推进、硬期限不续，避免集中高频重试。

## 实际执行结果

| 检查 | 本轮结果 | 范围 |
|---|---|---|
| Go完整回归 | 596条测试通过记录、8包通过、0失败/跳过 | `go test -race -count=1 -json -timeout=3m ./...`；包含真实Rust媒体用例 |
| SIP／媒体端到端 | 11套 × 2种UDP模式，202次通过，0失败/跳过/超时/强制清理 | 编解码、ESL、快照、放音事件、收号、变量、WAV、XML和媒体交互 |
| Rust | 58单元测试＋1集成测试通过；clippy通过 | 现有媒体层全目标回归 |
| Python证据与目录工具 | 102项通过 | 原始ID、日志、源码过期、伪绿色、收据损坏与发布门禁 |
| 前端进展展示 | 22项通过；JS语法通过 | 新增／撤回、版本、哈希、计数、缺失文件和过期证据 |
| 原版基础协议对照 | 26探针：21限定通过、3明确差异、2身份／目录观察 | 3差异为OPTIONS能力声明；不折算成全部协议通过 |
| 原版业务对照 | 12组全部限定通过；4生命周期事件在所测路径无额外差异 | WAV真实波形/50连续包、API停音、按键、XML、批量变量12向量及快照11向量 |

构建及执行保存前后源码清单、二进制SHA、测试名、完整原日志和退出码。Go收据为 `go-0575215013e54f4382136386c593de01`，E2E收据为 `e2e-3a8ba89c092141aea985bc84401d3457`；可从运行验证JSON核对内嵌日志。测试数量包含Go子用例以及两种UDP模式，不等同独立兼容能力数。

## 发布后500路短时回归

已发布服务运行小电话、停止清理和500路压力测试：500路全部接通，持续10秒双向媒体，标称／实际发送／唯一收到均为500000包，未收到0包、发生器写错误0。115次状态读取全部成功，最长约8.05毫秒；测试停止、资源回收和报告读取均通过，原配置revision=10与峰值策略保持不变。

这是本机隔离实例的短时SIP/G.711媒体回归，不包含5000路、转码／会议／录音组合或原版成对性能比较，因此不改变容量条目的失败状态。测试在第一次1.8发布上运行，随后只将该实测记录内嵌文档，功能源码、Rust媒体和发生器均未改变。[查看实际测试摘要](testlab-verification-v1.8.json)。

## 仍未闭合的范围

当前仍有2846条未实现入口、3918条无当前本地执行证据，属于不同维度，不能相加。完整清单保留全部3985个ID、优先级、下一步动作、剩余差异和文档位置，绿色条目的完整契约缺口也保留。

- 本次原版活动通道快照有138个JSON字段，候选单腿夹具有12个字段；其余126个原版字段未齐全。原版完整状态机、caller profile、codec和计费变量仍待逐项实现。
- 文件loading到下一次查询之间立即挂机的短窗口，尚无持久媒体事件日志证明开始；不能保证全部原版事件完备性。完整事件字段、transfer退出park和恢复执行等路径仍待补。
- 注册服务器／位置数据库、多网关、originate、转接、完整菜单XML、出站ESL、实时ASR/LLM/TTS流、录音、会议、WebRTC/ICE/DTLS/SRTP、全部原模块宿主和C ABI均未因本轮通过而获得认证。
- 本轮没有5000路组合容量新通过证据；原容量失败仍显示红色。历史5000路发生器供给不足和写错误尚未获得当前完整门禁通过；单路对照及短时本地测试不能替代5000/10000路Linux验收。

[查看全部逐项待办](compatibility-worklist.md) · [下载全量CSV](compatibility-worklist.csv) · [本轮计数与原始ID](compatibility-progress.json) · [本地运行证据](runtime-verification.md) · [原版12组业务对照](ai-calling-verification-v1.8.md) · [26项原版协议对照](conformance-report.md) · [通道快照接口](channel-snapshot-reference.md) · [生命周期事件接口](event-lifecycle-reference.md)
