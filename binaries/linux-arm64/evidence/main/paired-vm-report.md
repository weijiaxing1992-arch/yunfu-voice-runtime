# 本批固定 FreeSWITCH 成对复验

已使用既有 Linux 候选在本项目隔离 Alpine 虚拟机完成 26 项固定探针，未重建或修改配置、密钥、源码及探针。

- 21 项限定范围通过；身份观察 1 项、API 目录观察 1 项。
- 3 项真实差异保留：SIP OPTIONS 的 UDP、TCP、TLS 能力头不一致；运行器退出 2，不表示全过。
- 本批对照分母 3999，完整合同通过仍为 0。原始结果见 paired-01.json，完整探针报告见 paired-01.md。
- FreeSWITCH 1.11.3 的 243 个参考文件运行前后全部匹配；1135 个候选/项目/脚本输入文件指纹未改变。
- 原版先确认 calls/channels 为零，再 SIGTERM 正常结束，关闭日志完整。候选监听和媒体端口释放，客体检查无本次候选或原版进程。
- 虚拟机已 sync/poweroff，宿主终端退出 0，随后进程查询确认虚拟机已不存在；没有强杀。

候选脚本等待了其启动进程，但没有保存候选退出码；原版清理器并非其父进程，也未取得 wait 退出码。这里以实际进程消失、端口回收及原版正常关闭日志说明清理结果，不补造进程退出码。

复验汇总和全部产物摘要见 paired-vm-receipt.json。没有触碰宿主主服务 9080，没有执行后台文档生成。此结果不证明完整 FreeSWITCH 兼容、ASR 供应商接入或 5000/10000 路容量。
