# 贡献说明

欢迎围绕语音智能体的线路注册、呼叫、实时媒体、收号、IVR、资源保护和可重复验收提交改进。

1. 先查看 `13-项目未完成清单.md` 和对应接口合同，说明具体缺口与预期行为。
2. 默认修改 `source/main/`；ASR 候选仍在 `source/asr-candidate/`。合并候选时必须逐项处理接口、证据与回归，避免复制整个候选覆盖主工程。
3. 添加能重现问题的必要测试，保留失败结果。编译成功、工具退出成功、历史通过和完整业务验收必须区分。
4. 新增或修改逻辑使用中文注释解释约束与原因，并同步接口文档、字段字典和兼容对照。
5. 不提交运行凭据、客户号码、音频、令牌、密钥、生成缓存、生产配置或构建目录。测试示例使用回环或文档地址。
6. 提交 Pull Request 时写明问题、行为变化、验证命令、结果与未覆盖范围。性能结论应绑定代码、硬件、负载、音频及完整报告。

你提交的原创贡献按本项目 Apache-2.0 许可提供；引入第三方代码必须保留版权、许可证并更新第三方清单。不要修改第三方许可归属，或使用无法确认来源的代码/语音资产。

`manifest.json` 与 `source/*-manifest.json` 记录首版分发完整性，开发修改后校验失败是预期现象；发版时重新生成清单并记录变化。不要通过修改历史证据来使兼容门禁变绿。
