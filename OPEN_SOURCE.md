# FreeSWITCH 平替 RustSwitch · 开源说明

RustSwitch（工程与交付资料中也使用云蝠 Voice Runtime 名称）面向电话外呼、呼入与语音智能体。媒体处理采用 Rust，呼叫控制和管理后台采用 Go，通过独立 C ABI 与现有编解码库衔接。项目逐步提供有实际业务价值的 FreeSWITCH 接口兼容。

本次公开的是已有工程、资料、测试依据与缺口清单，供学习、开发、复现和协作。当前发布为开发预览。已有可编译、可运行的实现，同时存在未完成和未通过当前验收的部分。公开源码不表示通过生产验收，也不表示 100% FreeSWITCH 兼容。

## 本次开源清单

| 材料 | 仓库位置或发布方式 | 内容与状态 |
| --- | --- | --- |
| 主项目源码 | `source/main/` | Rust 媒体、Go 控制、管理页面、配置、构建工具和现有测试 |
| ASR 候选源码 | `source/asr-candidate/` | 独立候选快照，包含 ASR1 合同与 mock；尚未合并为主版本，不等于真实识别模型接入 |
| 已验证与工作中补丁 | `source/patches/` | 保留已测试快照及后续工作快照，按各自说明区分验证范围 |
| 设计参考 | `source/design-reference/` | 协议夹具和离线设计测试，不能当成已接入生产服务 |
| 项目资料 | 根目录 `01-` 至 `13-` 章节、`index.html`、`reference/` | 项目说明、拓扑图、架构图、接口文档、使用说明、运维与压测说明、需求和未完成清单 |
| 第三方构建源码 | `dependencies/` | 39 个 Rust 锁定依赖及固定 Opus 源码归档；Go vendor 与 SpanDSP 子集保留在各源码树中 |
| 源码与完整交付资源包 | GitHub Releases 附件 | 便于完整下载；附文件清单及校验值 |
| 编译运行包 | GitHub Releases 附件 | macOS ARM64 与 Linux ARM64；适用系统、原生库和验证边界见各包 README |
| 开源与版权说明 | `LICENSE`、`NOTICE`、`LICENSE_SCOPE.md`、`THIRD_PARTY_NOTICES.md` | 自有材料许可、第三方例外、来源和分发要求 |

本次开源材料包括源文件、文档可编辑原稿、历史验收记录和当前问题清单。公开版对个人本地路径及非必要的运行信息进行适当处理；公开版文件清单用于校验公开内容，不能替代原始验收记录的源码绑定。

## 授权方式

可由本项目授权的原创代码与原创文档采用 [Apache License 2.0](LICENSE)。使用、修改、分发和商业使用须遵守许可中的条件。第三方材料保持上游许可；本项目不重新授权第三方版权。

`native/audio/g722_support.c` 作为 G.722 独立动态库的一部分，单独采用 LGPL-2.1-only。SpanDSP、FreeSWITCH 模板、Opus、Go 与 Rust 依赖分别按 [许可范围](LICENSE_SCOPE.md) 和 [第三方说明](THIRD_PARTY_NOTICES.md) 处理。不能把包含这些材料的整个资源包统称为“全部 Apache-2.0”。

软件许可不授予项目名称、标识或第三方商标的背书权。FreeSWITCH 名称用于来源说明和兼容对照；本项目不是 FreeSWITCH 官方发行版，不宣称获得其官方认证。

## 当前验证边界

- 完整对照目录记录 3,999 项声明、接口、配置和验收条目，不等于 3,999 个已实现功能。应同时查看实现状态、证据范围与源码是否仍匹配。
- 当前资料的发布验收仍有阻塞：主版本文档校验提示 `key-api-pcm-stream` 绿色证据失效，ASR 候选文档字段字典与机器合同不一致。修复前保留为未通过。
- 历史测试通过只覆盖相应快照、平台和场景。不能直接推出当前版本的完整 FreeSWITCH 兼容、5,000／10,000 路生产容量或真实语音智能体全链路性能。
- Linux ARM64 主版本具有限定范围运行记录；候选运行包组合尚未共同运行验收。macOS 原生库的系统要求与核心程序不同，随包 README 明确最低系统版本。
- 真实 ASR/TTS 服务、完整 Agent 编排、部分通信协议、编解码及传统 PBX 功能仍按未完成清单推进。

## 参与方式

请使用仓库 Issues 描述问题和复现条件，使用 Pull Request 提交改进。功能变更应同时更新接口合同、中文说明和有意义的验证证据。对比 FreeSWITCH 时固定原版提交与运行配置，明确相同项、差异项、未验证项；只将实际通过且证据仍有效的范围标为通过。

提交者应确认有权提交相应内容，保留第三方版权和来源，并沿用被修改文件的许可。新的依赖、测试资源和编译附件应补充来源、版本、许可与校验值。不要在公开问题和补丁中提交真实线路密码、访问令牌、个人录音或其他无关私有数据。
