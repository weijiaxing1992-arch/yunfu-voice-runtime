# 13 项目未完成清单

整理日期：2026-09-08。本文作为 RustSwitch 的研发工作登记，记录功能缺口、已有证据、代码入口、依赖和完成条件。适用于研发分工、测试设计及版本评审，覆盖语音智能体核心能力与渐进式 FreeSWITCH 兼容。

清单保留 40 个稳定工作包编号；其中 DLV-04 的公开许可与分发材料已完成，其余条目按各自实现和验证状态推进。3,999 条原始对照及历史测试记录单独保留，不以工作包数量计算兼容率。版本和历史证据规则见[文档中心](DOCUMENTATION.md)。

配套资料：[交付与源码说明](11-交付与源码说明.md)、[需求说明书](12-需求说明书.md)、[完整3999行JSON](backlog/FreeSWITCH-全部逐项状态.json)、[完整CSV](backlog/FreeSWITCH-全部逐项状态.csv)、[未实现与部分实现2897行](backlog/未实现与部分实现.csv)、[未通过本地验证3917行](backlog/未通过本地验证.csv)。原始参考：[对照数据](reference/docs/api/comparison.json)、[运行验证数据](reference/docs/api/runtime-verification.json)、[兼容工作清单](reference/docs/api/compatibility-worklist.md)、[Voice Runtime V1验收](reference/docs/api/voice-runtime-v1-acceptance.md)。

## 一、实现与交付基线

1. **主源码对照快照**：3999条对照记录，历史本地passed为82条；这仅是文件中的存量状态。**预览交付时功能文档门禁未通过，当前有效通过条目仍须重新核验**。主源码失败原因为“绿色已失效：key-api-pcm-stream”，ASR候选失败原因为“字段字典落后于机器合同”。原日志位于交付包 `evidence/delivery-doc-gate`，当前有效绿色总数尚待重新评估。
2. **ASR隔离候选**：已有内部ASR1、时效/生命周期保护、重采样、来源对账和资源统计；`full-review-01`实际完成1471个通过测试事件（其中504个顶层测试），0失败/跳过，含限定本机真实链路、race、vet、页面检查与双平台编译。候选源码已随开发预览公开交付，尚未合入主源码或完成产品发布验收，不包含真实识别模型。CHECKPOINT中较早“回归运行中”的记录由实际完成收据更新解释。
3. **独立压测修复草案**：test-02对应的六文件可按SHA恢复，并有定向回归；当前工作稿后来改动runner和test两文件，没有test-03收据。原子Stats方案仅有合同，无实现和验证，不能列为已修复。
4. **预览交付构建**：两套源码的Go/Rust/C ABI离线编译已通过，候选另含asr-mock；编译通过不等于文档发布门禁或容量验收通过。本次公开分发已新增项目Apache-2.0许可与第三方例外说明，原历史审查时尚无根许可。本文未操作主服务、未部署，也未重新读取在线服务版本。

## 二、3999条原始状态统计

参考：FreeSWITCH 1.11.3，提交 `ef32e205295e29f034f1453ad245ba5efb07b94a`。以下保留原JSON状态，不按工作包重新归类或删减分母。

| 实现状态 | 原始标识 | 条数 |
|---|---|---:|
| 未实现 | `not_implemented` | 2844 |
| 部分实现 | `partial` | 53 |
| 实现状态未验证 | `not_verified` | 178 |
| 仅配置导出 | `export_only` | 882 |
| 自有接口已实现 | `implemented` | 19 |
| 内部接口 | `internal_only` | 23 |
| 合计 | | 3999 |

| 存量本地验证状态 | 条数 | 当前解释 |
|---|---:|---|
| passed | 82 | 限定断言的历史通过记录；预览功能门禁未通过，当前有效性待复核 |
| failed | 1 | 失败保留，不能用代码存在替代验收 |
| not_run | 3916 | 没有满足该条目门禁的本地运行证据 |
| 合计 | 3999 | 实现状态与验证状态是两个维度 |

未实现+部分实现为2844+53=2897行；未通过本地验证为1+3916=3917行。882条配置导出由875条vanilla参数与7条关键映射组成；177条验收定义由169场景和8容量profile组成，当前均为not_run。144个`fs_module`和1865个原生函数声明不能按独立业务模块相加计算兼容率。`implemented`指自有入口已实现，`internal_only`不是公共HTTP或FreeSWITCH线协议兼容。

运行证据文件的 `paired_status=not_run`、`upstream_runtime_executed=false`、`production_capacity_certified=false` 仍保留。历史部分模块存在原版隔离实验，不等于该份运行证据完成全部原版成对验收。当前没有可信全量兼容百分比。

### 按原始类别展开

| 类别 | 总数 | 未实现 | 部分 | 未验证 | 仅导出 | 自有接口 | 内部接口 | 历史passed | failed | not_run |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| acceptance_definition | 177 | 0 | 0 | 177 | 0 | 0 | 0 | 0 | 0 | 177 |
| fs_api | 292 | 279 | 13 | 0 | 0 | 0 | 0 | 8 | 0 | 284 |
| fs_application | 222 | 214 | 8 | 0 | 0 | 0 | 0 | 8 | 0 | 214 |
| fs_channel_variable | 100 | 100 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 100 |
| fs_chat_application | 14 | 14 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 14 |
| fs_conference_subcommand | 90 | 90 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 90 |
| fs_event | 94 | 83 | 11 | 0 | 0 | 0 | 0 | 10 | 0 | 84 |
| fs_json_api | 10 | 10 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 10 |
| fs_module | 144 | 144 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 144 |
| fs_native_function | 1865 | 1865 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 1865 |
| fs_sofia_subcommand | 40 | 40 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 40 |
| fs_vanilla_parameter | 875 | 0 | 0 | 0 | 875 | 0 | 0 | 0 | 0 | 875 |
| key_api | 2 | 0 | 0 | 0 | 0 | 1 | 1 | 2 | 0 | 0 |
| key_c_abi | 2 | 1 | 1 | 0 | 0 | 0 | 0 | 0 | 0 | 2 |
| key_cli | 3 | 0 | 1 | 0 | 0 | 2 | 0 | 1 | 0 | 2 |
| key_config | 7 | 0 | 1 | 0 | 5 | 1 | 0 | 5 | 0 | 2 |
| key_esl | 7 | 1 | 5 | 0 | 0 | 1 | 0 | 6 | 0 | 1 |
| key_guard | 6 | 0 | 1 | 0 | 0 | 5 | 0 | 5 | 0 | 1 |
| key_http | 9 | 0 | 0 | 0 | 0 | 9 | 0 | 8 | 0 | 1 |
| key_ipc | 22 | 0 | 0 | 0 | 0 | 0 | 22 | 19 | 0 | 3 |
| key_media | 10 | 1 | 6 | 1 | 2 | 0 | 0 | 5 | 1 | 4 |
| key_sip | 8 | 2 | 6 | 0 | 0 | 0 | 0 | 5 | 0 | 3 |

## 三、当前已确认的失败与证据限制

历史2000路转发任务 `a4eb04b740b16479113f91b1c78e6d41` 建立2000路，媒体10秒发送2,000,000包，仅接收456,233个唯一包，缺失1,543,767，任务失败。10个Rust worker缓存累计RX/TX均约921,898；最终HTTP返回的worker发布时间比发生器退出早约729–735ms，不能用它定位整个测试的最终损失。控制器活动数为0而旧媒体缓存为2000也不能单独判通道泄漏。

该历史任务的 Darwin portable 路径发生器为8发送worker、256接收reader，服务侧每批约1包；256次末尾超时不等于256次读错误。所有分片的内核socket丢弃计数均不支持，原始0值不能说明内核无丢包。发生器约12 CPU秒包含20秒建呼等全生命周期，Rust累计约6 CPU秒；不能直接除媒体10秒后宣称精确媒体CPU率，更不能断定10核饱和。需先修同源统计和未知计数，再用目标Linux环境定位。

本机5000路任务曾因隔离端口不足拒绝完整运行，该结果不构成5000路或10000路能力验证。ASR的9项Unix测试与5项SIP/Rust测试仅验证内部ASR1和mock；现有真实BYE用例不覆盖等待final期间挂断。Linux二进制构建、DSP微基准和本机有限回归均不能外推Linux容量、真实模型或生产无损切换。

## 四、逐项工作包

P0优先处理可信交付、核心AI业务和容量承诺；P1推进电话接入、运维与渐进兼容；P2可按首期业务延期，但不得隐藏差异。以下代码位置是接手入口，不表示标题功能已经实现。普通代码路径相对公开候选目录 `source/asr-candidate/`；主源码对应入口位于 `source/main/`。`private/` 是历史独立补丁的来源标签，相关交付文件见[源码说明](11-交付与源码说明.md)，并非公共代码目录。DLV-04 的项目级许可材料位于仓库根目录。

### DLV-01 · P0 · ASR隔离候选合并、文档和发布

**状态：**候选限定实现已公开交付，待合入主源码并完成验收。**验证：**限定回归通过，正式文档尚未接入。

真实 Unix 9 例、真实 SIP/Rust 5 例及整包回归已通过；供应商为校验 PCM 的 mock，不是识别模型。

代码入口：`control/internal/asr`；`control/internal/server/asr_stream.go`；`control/internal/server/asr_observe.go`。

依赖：DLV-03；冻结源码和发布窗口。

验收方法：核对整包收据与源码，合并后重跑受影响检查、文档门禁，并核验实际运行二进制和页面。

完成条件：源码、文档、原始证据、构建和发布收据一致；仍明确内部 SDK 与 mock 范围。

对照ID：`key-api-rx-sdk`, `key-http-status`, `key-http-metrics`；验收/目标索引：`A09-ASR`, `V1-08`, `V1-16`。

### DLV-02 · P0 · RX时效和通话生命周期增量发布

**状态：**候选限定实现已公开交付，待合入主源码并完成验收。**验证：**候选回归通过，未部署。

候选已有 LifetimeDone 和 RemainingBudget；主动退订不等于通话结束，100ms 预算不能重置。

代码入口：`control/internal/media/rx_clock.go`；`control/internal/media/rx_stream.go`；`control/internal/server/rx_stream.go`。

依赖：DLV-01。

验收方法：验证正常 Finish/Unsubscribe 可等 final/DONE，真实挂断、代次变化、替换与服务停止及时撤权；旧音频绝对期限不续期。

完成条件：发布后保留两种生命周期边界和期限失败原件，不能将 summary 当新音频预算。

对照ID：`key-api-rx-sdk`, `key-ipc-rx-stream`；验收/目标索引：`A05`, `A09-ASR`。

### DLV-03 · P0 · 修复两套源码的文档发布门禁

**状态：**未完成。**验证：**预览交付时两套功能文档门禁未通过。

主源码检查报“绿色已失效：key-api-pcm-stream”；ASR 候选报“字段字典落后于机器合同”。历史 82 条 passed 不能当当前有效绿色总数。

代码入口：`tools/build_api_docs.py`；`docs/api`。

依赖：证据有效期/源码绑定核查；候选字段字典更新。

验收方法：分别对打包主源码和候选运行 python3 -B tools/build_api_docs.py --check；按失效原因补真实证据或降级状态，禁止只延长过期时间。

完成条件：两套门禁通过且页面、机器合同、字段字典、证据与实际源码一致；当前可绿数量重新计算。

对照ID：`key-api-pcm-stream`；验收/目标索引：`发布门禁`。

### DLV-04 · P0 · 确定项目自有源码许可和分发范围

**状态：**公开许可与分发材料已完成。**依据：**2026-09-08 开发预览已提供项目 Apache-2.0 `LICENSE`、`NOTICE`、第三方清单及相应许可文本。

初始清单记录的“许可未确定”属于公开分发前的历史状态；原始快照保留用于追溯。本项完成仅涉及许可和分发材料，功能与容量验收独立处理。

材料入口：[开源说明](OPEN_SOURCE.md)、[许可范围](LICENSE_SCOPE.md)、[第三方清单](THIRD_PARTY_NOTICES.md)。

依赖：项目权利人决定许可、商业分发边界；第三方许可复核。

验收方法：确认代码归属、项目许可文本、codec/依赖再分发要求和归属说明，检查源码包与二进制包。

完成条件：由有权人员确定项目许可并随交付提供，保留第三方许可与必要声明。

验收/目标索引：`交付要求`。

### DIA-01 · P0 · Stats原子快照与真正退出后采集边界

**状态：**未实现。**验证：**已确证现有口径缺陷，修复未验证。

当前 Stats 和 Admission 分别发布/读取。旧 RPC 结果可在发生器退出后发布，SampledAt 新不能证明 Rust 在退出后生成统计。原子方案只有合同，无实现目录和测试收据。

代码入口：`control/internal/media/pool.go`；`control/internal/server/server.go`；`control/internal/server/benchmark_runner.go`。

依赖：确定原子 Stats、请求开始/返回时间、PID、generation 字段合同。

验收方法：注入退出前请求退出后返回、跨代更新、分片缺失、旧字段缺失和时间异常；成功验收与失败诊断使用同一新鲜判据。

完成条件：每个预期 worker 的原子行身份一致，请求开始晚于发生器退出且返回不早于开始；缺失或旧格式为未知。

对照ID：`key-ipc-stats`, `key-http-status`, `key-media-capacity`；验收/目标索引：`V1-16`, `V1-17`。

### DIA-02 · P0 · 失败末尾有界采样与取消终态

**状态：**部分实现于已公开独立补丁，尚未合入主工程。**验证：**test-02限定通过，后改两文件未复验。

已公开独立补丁最多补采样5秒，继承取消、保留最后有效样本和原失败；旧判据仍依赖 Admission。后加 benchmarkContextOutcome 和8项用例未有 test-03。

代码入口：`private/control/internal/server/benchmark_runner.go`；`private/control/internal/server/benchmark_failure_diagnostics_test.go`。

依赖：DIA-01；冻结后增量回归。

验收方法：回归原失败叠加取消/期限、成功后取消、服务停止、HTTP失败及样本/时间配对；验证真实 defer 使用终态归一逻辑。

完成条件：不改变原失败，不阻塞主状态接口，任务取消及时结束；全部预期分片新鲜、超时、取消分开报告；当前完整补丁有新收据。

对照ID：`key-cli-callbench`, `key-media-capacity`；验收/目标索引：`V1-17`。

### DIA-03 · P0 · 不支持的socket丢弃计数显示未知

**状态：**独立补丁已公开，待合入主工程。**验证：**test-02限定回归通过。

已传递 socket_drop_counter_supported 和支持分片数；旧报告缺字段、部分支持或全不支持不能把0显示为无丢包。

代码入口：`private/control/internal/server/benchmark.go`；`private/control/internal/server/benchmark_processing.go`；`private/control/internal/server/web/tests.js`。

依赖：从已验证 test-02 六文件恢复范围选择补丁；集成回归。

验收方法：验证新旧报告、全支持/部分/不支持、0与非0计数、字段缺失；原始计数和可观测范围同时展示。

完成条件：合并后报告及页面对未知显示“未提供”，部分计数不伪称完整计数；本项不能替代DIA-01。

对照ID：`key-http-status`, `key-cli-callbench`；验收/目标索引：`V1-16`, `V1-17`。

### AI-01 · P0 · 真实ASR供应商与识别质量

**状态：**未实现真实模型接入。**验证：**内部ASR1链路有限通过，识别未验证。

当前内部 Server.StartASR 和私有 Unix ASR1 已实现；mock 只校验音频/协议，不识别语音，不是公共 HTTP API。

代码入口：`control/internal/asr/stream.go`；`control/internal/asr/wire`；`control/cmd/asr-mock`。

依赖：供应商协议、模型版本、凭证、语料与质量门槛。

验收方法：对真实供应商跑流式 partial/final、错误、超时、取消、慢读与连接恢复；真实语料核对文字来源和时延。

完成条件：真实识别模型输出可追溯到音频范围；准确率、首字/最终时延、丢弃和资源成本达到约定值。

对照ID：`key-api-rx-sdk`；验收/目标索引：`A09-ASR`, `V1-08`。

### AI-02 · P0 · 实时VAD与语音端点检测

**状态：**未实现。**验证：**未验证。

已有丢包、PLC、CN和来源元数据，不等于具备VAD和ASR端点算法。

代码入口：`media/src/audio`；`control/internal/asr/frames.go`。

依赖：算法/模型与阈值；噪声回声测试集。

验收方法：测试噪声、短句、连续说话、CN、丢包、播放回声及用户同时说话；对照媒体时间和人工标签。

完成条件：可输出有来源时间的语音起止，丢包不被当沉默；误检/漏检和端点时延有测量及有界配置。

验收/目标索引：`V1-07`, `EV06`, `EV07`, `A09-ASR`。

### AI-03 · P0 · 真实流式TTS适配

**状态：**未实现供应商接入。**验证：**下行PCM基础有限验证。

RVA1和PCM轮次下发已提供基础，不等于真实流式TTS供应商适配。

代码入口：`control/internal/server/pcm_endpoint.go`；`control/internal/server/pcm_stream.go`；`media/src/media/worker_pcm.rs`。

依赖：TTS供应商、格式、流协议、取消语义。

验收方法：测试首块、分块、突发、慢发送、部分写、超时及取消后旧块；验证字节和毫秒双重队列上限。

完成条件：真实TTS可边生成边播放；迟到旧块不复活，媒体worker不等待模型或网络。

对照ID：`key-api-pcm-stream`, `key-ipc-pcm-push`；验收/目标索引：`A09-TTS`, `V1-09`, `CMD04`。

### AI-04 · P0 · Agent/LLM轮次编排与上下文更新

**状态：**未实现。**验证：**未验证。

PCM轮次及ASR状态不是完整Agent编排，尚无真实ASR→LLM→TTS上下文循环。

代码入口：`control/internal/server`；`control/internal/asr/types.go`。

依赖：AI-01、AI-03；模型与上下文版本合同。

验收方法：重复多轮对话，注入迟到模型响应、上下文更新、取消和并发会话，检查每个事件的session/turn/version。

完成条件：会话隔离、旧轮次不可覆盖新上下文，错误/取消和完成事件可对账。

验收/目标索引：`CMD09`, `V1-15`, `A14`。

### AI-05 · P0 · 语音自动打断与旧轮次撤销

**状态：**部分实现。**验证：**PCM中断/渐变有限验证，完整语音打断未验证。

已有PCM轮次中断和0/20/40ms渐变；没有完整VAD触发、LLM/TTS取消与旧结果撤销闭环。

代码入口：`media/src/media/pcm_turn.rs`；`control/internal/server/pcm_stream.go`；`control/internal/asr/stream.go`。

依赖：AI-02、AI-03、AI-04。

验收方法：播放时说话、连续打断、旧TTS迟到，测最后旧媒体发送时间和用户实际听到的声音；覆盖取消竞态。

完成条件：所有旧轮次结果失权，打断时延和声学效果达到约定，不能仅以控制API返回成功判通过。

对照ID：`key-ipc-pcm-turn-interrupt`；验收/目标索引：`V1-11`, `V1-12`, `EV08`, `A10`。

### AI-06 · P0 · 对齐双轨录音与异步持久化

**状态：**仅配置导出，运行链路未实现。**验证：**未验证。

录音配置/映射不等于RX/TX双轨录音、时间对齐、文件完成与上传功能。

代码入口：`media/src/media/processed.rs`；`media/src/media/worker_pcm.rs`；`control/internal/server/esl_api.go`。

依赖：录音格式、存储和保留策略。

验收方法：真实通话同时采集RX/TX并核对样本/时间/文件摘要；注入磁盘慢/满、上传失败及挂断。

完成条件：双轨可重放并对齐，完成和失败可见；媒体线程不等磁盘或上传，资源及时释放。

对照ID：`key-media-recording`；验收/目标索引：`V1-13`, `CMD07`, `CMD08`, `A13`。

### AI-07 · P0 · 统一10命令和11事件控制合同

**状态：**部分实现。**验证：**完整业务合同未验证。

已有HTTP、ESL和IPC子集，不代表新的10命令/11事件全部完成；JobUUID不自动提供业务幂等。

代码入口：`control/internal/server/esl_api.go`；`control/internal/server/server.go`；`control/internal/asr/types.go`。

依赖：各AI模块、版本及事件序列规范。

验收方法：逐个实例化接受/执行/完成/失败/超时/取消、重复请求、事件背压和重连恢复用例。

完成条件：每项有真实业务效果和可对账结果，未知操作明确拒绝，无仅返回OK的空实现。

对照ID：`key-esl-api`, `key-esl-bgapi`, `key-http-concurrency`；验收/目标索引：`A14`, `CMD01–CMD10`, `EV01–EV11`, `V1-15`。

### AI-08 · P0 · 一通完整真实AI电话闭环

**状态：**未完成集成。**验证：**当前模拟供应商SIP路径通过，真实业务未验证。

已有5项真实SIP/Rust/ASR1用例；mock不识别。现有BYE发生在Finish/DONE和资源关闭后，不能证明等待final时的真实挂断窗口。

代码入口：`control/internal/server/asr_stream_real_test.go`；`control/internal/server/pcm_stream.go`。

依赖：AI-01至AI-06；真实线路。

验收方法：外呼/呼入、识别、回答、DTMF/IVR、语音打断、录音和挂断；专测final等待期间实际BYE及错误恢复。

完成条件：原始音频、SIP、模型事件、轮次和资源清理贯通，所有关键故障分支有真实证据。

验收/目标索引：`M2`, `V1-07`, `V1-08`, `V1-09`, `V1-11`, `V1-13`。

### MED-01 · P0 · G.722与Opus实时媒体图

**状态：**部分实现。**验证：**离线/透传与实时处理必须分开。

现有离线/原生编解码基础或RTP透传不能证明通话中完成G.722/Opus解码、处理、编码。

代码入口：`media/src/audio/native.rs`；`media/src/audio/branch.rs`；`media/src/media/processed.rs`。

依赖：固定编解码库版本和许可；实时packetizer/depacketizer。

验收方法：逐codec真实双向通话和独立PCM oracle；G.722按16k音频/8k RTP时钟，Opus按48k RTP时钟，覆盖包长/绕回/损坏。

完成条件：真实处理链质量、时钟、时延和CPU预算通过，状态表明确透传与转码差别。

对照ID：`key-media-transcoding`, `key-http-codecs`；验收/目标索引：`A06`, `V1-02`, `M1`。

### MED-02 · P1 · G.729/G.726及移动音频兼容

**状态：**部分实现/预留。**验证：**运营商互通和完整转码未验证。

Payload配置/透传不能当G.729/G.726实时转码；AMR-NB/WB、EVS按IMS接入需求继续演进。

代码入口：`control/internal/codecprofile`；`media/src/codec.rs`；`media/src/audio/native.rs`。

依赖：线路真实codec需求；库和专利/分发许可。

验收方法：按codec逐项测试SDP、时钟、帧长、动态Payload、真实解编码与运营商样本；分别记录支持层级。

完成条件：当前业务必需codec可实际互通；可选codec未做时保留不支持状态，不宣传已具备。

对照ID：`key-media-rtp`, `key-c_abi-codec`；验收/目标索引：`A06`。

### MED-03 · P0 · 抖动、丢包、CN/DTX/PLC/FEC策略

**状态：**部分实现。**验证：**限定G.711路径通过，完整策略未验证。

已有有界处理和PLC/CN元数据；完整自适应策略及各codec的FEC/DTX尚需实现与验收。

代码入口：`media/src/media/jitter.rs`；`media/src/media/processed.rs`；`control/internal/asr/provenance.go`。

依赖：故障发生器、codec能力矩阵、语音质量语料。

验收方法：注入迟到、突发丢失、乱序、序号绕回、CN转语音，测试不同延迟档位；对账全部丢弃与媒体时间。

完成条件：不凭空造新鲜音频或将丢包当沉默；延迟与质量策略明确、有上限且有真实测量。

对照ID：`key-media-rtp`, `key-media-transcoding`；验收/目标索引：`A11`, `V1-04`, `V1-05`。

### MED-04 · P1 · RTCP mux、安全反馈与质量事件

**状态：**部分实现。**验证：**有限报告路径已测，扩展未验证。

已有RTCP转发或G.711 SR/RR/SDES/BYE；不等于mux、SRTCP、完整反馈和自适应报告周期。

代码入口：`media/src/media/rtcp.rs`；`media/src/media/worker.rs`。

依赖：SEC-01；质量事件定义。

验收方法：真实复合报告、mux、加密/重放、反馈和周期变化，区分网络抖动、排队和调度迟到。

完成条件：相关协议与质量事件均有独立对端和原始报文验收。

对照ID：`key-media-rtcp`；验收/目标索引：`V1-01`, `V1-16`。

### MED-05 · P1 · 通用静态AudioGraph与有界内存

**状态：**部分实现。**验证：**限定分支通过，完整图未验证。

有固定媒体分支、AudioFrame和127抽头8→16k重采样；不能外推所有节点、整条链零分配或任意图性能。

代码入口：`media/src/audio`；`media/src/media/processed.rs`；`control/internal/asr/resample`。

依赖：AI分支合同、静态模板和内存预算。

验收方法：对慢分支、取消、版本切换测队列年龄、复制、分配和锁等待；重采样与独立oracle及频响比较。

完成条件：所支持静态图的历史/输出/队列有硬上限，任一慢分支不能拖垮主媒体。

验收/目标索引：`A07`, `A08`, `A16`, `V1-05`, `V1-06`。

### CAP-01 · P0 · 定位2000路历史转发失败

**状态：**未完成优化。**验证：**真实失败。

2,000,000发送仅456,233唯一接收；末端服务统计是早约729–735ms的缓存。没有证据定位每一跳丢失。

代码入口：`control/cmd/callbench`；`media/src/media/io.rs`。

依赖：DIA-01至DIA-03；隔离网络与资源预算。

验收方法：采集真实socket缓冲、计数可用性、各阶段一致时间窗口，重跑同档位并对比独立Linux发生器与portable路径。

完成条件：可解释丢失来源并通过原阈值；不能靠减少实际媒体量、修改阈值或把尾部超时当读错误来修报告。

对照ID：`key-cli-callbench`, `key-media-capacity`；验收/目标索引：`V1-17`, `M1`。

### CAP-02 · P0 · Linux 1000/5000/10000路转发容量

**状态：**未验证。**验证：**尚无目标Linux容量验收通过。

Linux交叉编译通过不等于运行容量；本机5000路隔离端口不足的拒绝不是已完成压测。

代码入口：`config/linux-10000.example.json`；`control/cmd/callbench`；`media/src/media/worker.rs`。

依赖：用户提供Linux CPU/NUMA/内存/网卡；独立发生器、地址/端口和FD预算。

验收方法：按1000→5000→10000真实双向媒体逐档测试，保存完整负载、唯一包、时间、资源、错误和清理证据。

完成条件：每档足量、完整运行、符合指标，任何阶段失败保留失败；不得把建呼数代替媒体服务数。

对照ID：`key-media-capacity`；验收/目标索引：`V1-01`, `V1-17`, `M1`。

### CAP-03 · P0 · Linux实时G.711及核心codec容量

**状态：**未验证。**验证：**转发结果不能代替解码/编码结果。

真实decode→PCM→encode的1000/5000/10000路容量仍需验收，且要计入重采样和必要分支成本。

代码入口：`media/src/media/processed.rs`；`control/cmd/callbench/media_processed.go`。

依赖：CAP-02、MED-01；独立样本oracle。

验收方法：逐档、逐codec实际解码编码，核对PCM内容、RTP时间、PLC/丢弃、发送迟到和资源。

完成条件：每条流都有真实处理及正确样本，稳定服务能力和资源在同口径下达到门槛。

对照ID：`key-media-capacity`, `key-media-transcoding`；验收/目标索引：`M1`, `V1-02`, `V1-17`。

### CAP-04 · P0 · 完整Agent等价成本与容量优势

**状态：**未验证。**验证：**性能目标未获真实证明。

DSP微基准和relay容量都不能证明完整Agent优势；真实ASR/LLM/TTS/录音尚未组成等价负载。

代码入口：`control/internal/asr`；`control/internal/server/pcm_stream.go`。

依赖：AI-08、CAP-03；固定FreeSWITCH基线与等价业务。

验收方法：同机、同音频、同质量、同并发和SLA对比，计入Go/Rust/SIP或SBC、DSP、适配器、录音上传与内核成本。

完成条件：证明至少1.5倍稳定容量目标（进阶2倍），同并发CPU/内存各降30%目标及TX迟到P99≤2ms/P99.9≤5ms；不调整口径制造优势。

对照ID：`key-media-capacity`；验收/目标索引：`M2`, `M3`, `V1-17`。

### CAP-05 · P0 · 72小时混合负载与故障恢复

**状态：**未验证。**验证：**尚无生产级长稳认证。

局部限流和清理测试不能替代72小时负载、资源回收和故障恢复。

代码入口：`control/internal/server`；`control/internal/media/pool.go`；`media/src/media/worker.rs`。

依赖：AI-08、CAP-04；故障注入环境。

验收方法：突发建呼、ASR停读、TTS突发、慢盘、网络异常、worker退出、端口复用，持续观测线程、内存和最忙分片。

完成条件：72小时满足混合负载与稳定门槛，故障影响和恢复时间可量化，不通过少接业务掩盖服务损失。

对照ID：`key-guard-capacity`, `key-media-failover`；验收/目标索引：`M3`, `M4`, `A17`。

### SIP-01 · P1 · 真实运营商注册与多网关

**状态：**部分实现。**验证：**固定上游客户端子集通过，现网互通未验证。

已有Digest、刷新、423、退避和退订等客户端能力；没有完整registrar/location、多账号网关、DNS和Sofia运行时。

代码入口：`control/internal/server/trunk_registration.go`；`control/internal/server/trunk_auth.go`。

依赖：真实线路账号、运营商认证规格和网关需求。

验收方法：对真实运营商验证鉴权、刷新、挑战重试、失效回退与呼叫；多账号、注册服务端分别验收。

完成条件：所需线路稳定注册并可实际呼叫；不将客户端注册能力宣称成完整注册服务器。

对照ID：`key-sip-register`；验收/目标索引：`A15`。

### SIP-02 · P1 · 主动外呼、路由、桥接与转接

**状态：**部分实现。**验证：**业务/API完整语义未验证。

固定上游和有限本地动作不等于完整originate、多路路由、bridge或transfer。

代码入口：`control/internal/server/esl_api.go`；`control/internal/server/server.go`；`control/internal/server/dialplan.go`。

依赖：SIP-01；路由、失败回退和呼叫状态合同。

验收方法：真实呼入/主动外呼及转接，对比信令、双向媒体、失败回退和资源原子释放，注入重复控制。

完成条件：业务命令真正改变对话和媒体，无重复外呼、僵尸通道或仅返回成功的占位实现。

对照ID：`key-sip-invite`, `key-esl-api`, `key-config-dialplan-runtime`；验收/目标索引：`CMD01`, `CMD06`, `A14`, `A15`。

### SIP-03 · P1 · 对话重协商与SIP扩展

**状态：**未实现/部分基础。**验证：**扩展未验证。

基础INVITE/ACK/CANCEL/BYE已覆盖子集；完整re-INVITE、UPDATE、PRACK、REFER、订阅及会话刷新不能据此标通过。

代码入口：`control/internal/server/server.go`；`control/internal/sip`。

依赖：固定RFC/FreeSWITCH行为与对端能力。

验收方法：逐场景测试保持恢复、转接、早期媒体、重传、迟到200及取消交叉；对比原始消息和实际媒体。

完成条件：所声明扩展均有对端互通和失败分支证据，未声明的明确拒绝。

对照ID：`key-sip-renegotiation`, `key-sip-extensions`, `key-sip-cancel`, `key-sip-bye`；验收/目标索引：`A15`。

### SIP-04 · P1 · IPv6、DNS/SRV及WS/WSS信令

**状态：**部分实现。**验证：**IPv4 UDP/TCP/TLS之外未完整验证。

已有IPv4 UDP及可选TCP/TLS；没有完整IPv6、DNS-SRV、WS/WSS、客户端证书及SIPS全语义。

代码入口：`control/internal/server/transport.go`；`control/internal/sip`。

依赖：传输方案、证书、DNS及浏览器对端。

验收方法：测试解析故障切换、连接身份、证书校验、分帧/背压和多地址会话，记录真实端点。

完成条件：协议矩阵逐项通过并保持连接资源上限；TLS信令不能当安全媒体通过。

对照ID：`key-sip-transports`, `key-sip-sdp`；验收/目标索引：`A15`。

### SIP-05 · P1 · NAT学习、端口迁移和移动终端

**状态：**部分实现。**验证：**固定来源校验通过，迁移未验证。

当前固定协商来源/CIDR及connected模式不等于NAT新源学习；端口变更会拒绝，内核拒绝也未必计入应用计数。

代码入口：`media/src/media/worker.rs`；`control/internal/sip/sdp.go`。

依赖：来源认证、安全学习或SBC方案。

验收方法：模拟对称NAT、重绑定、移动切换、伪造包和旧队列，核对来源切换与媒体连续性。

完成条件：授权端点可按合同迁移，攻击来源不能接管；拒绝计数注明内核/应用覆盖范围。

对照ID：`key-media-nat`；验收/目标索引：`A15`。

### SEC-01 · P1 · SRTP、DTLS、ICE和WebRTC安全媒体

**状态：**未实现。**验证：**未验证。

信令TLS不提供SRTP；当前没有完整DTLS-SRTP、ICE和安全媒体生命周期。

代码入口：`media/src/media/worker.rs`；`control/internal/server/transport.go`。

依赖：选择自实现或固定SBC组件；密钥与互通策略。

验收方法：真实密钥协商、重放/损坏包拒绝、ICE切换、重协商、超时清理及SRTCP，计入所有组件成本。

完成条件：端到端安全媒体和故障分支通过；复用外部SBC时明确依赖与成本，不能称内核原生已实现。

对照ID：`key-media-security`；验收/目标索引：`A15`。

### TEL-01 · P1 · 完整DTMF发送和采集语义

**状态：**部分实现。**验证：**本地A腿子集通过，其余未验证。

已有telephone-event及有限INFO接收、本地A腿发送；桥接/B腿发送、INFO发送、带内检测和完整长事件/格式仍有缺口。

代码入口：`media/src/media/dtmf_sender.rs`；`media/src/media/interaction.rs`；`control/internal/server/esl_send_dtmf.go`。

依赖：电话侧DTMF方式、桥接与发送合同。

验收方法：真实对端识别16个键，检查时钟、持续时间、结束重传、连续/重叠事件和取消；分别验证各腿和协议。

完成条件：业务真正收到正确按键且无重复、丢键或无限持续事件；各发送完成状态有实际依据。

对照ID：`key-media-dtmf`, `key-ipc-dtmf-send`, `key-esl-dtmf-send-status`；验收/目标索引：`V1-14`, `EV05`。

### TEL-02 · P1 · 完整IVR与XML拨号计划运行

**状态：**部分实现。**验证：**有限动作通过，完整FS行为未验证。

当前单RE2条件和answer/set/unset/read/playback/sleep/park/hangup等有限动作，不支持完整预处理、include、变量扩展、反动作、continue、reloadxml与全部应用。

代码入口：`control/internal/server/dialplan.go`；`control/internal/dialplan`；`control/internal/server/esl_applications.go`。

依赖：明确首批IVR动作/变量规则；TEL-01、SIP-02。

验收方法：真实菜单播放、超时重试、非法输入、打断、分支转接、挂断及热更新，逐项对比原版轨迹。

完成条件：支持动作真实执行，未支持动作明确报错；保存或导出XML不得算运行兼容。

对照ID：`key-config-dialplan-runtime`, `key-esl-sendmsg`；验收/目标索引：`A14`。

### FS-01 · P1 · ESL完整命令、事件与Outbound会话

**状态：**部分实现。**验证：**部分线协议通过，全集未验证。

已有有限framing/API/事件行为；Outbound ESL、全部输出格式、过滤、linger与连接语义仍存在缺口。

代码入口：`control/internal/esl`；`control/internal/server/esl_api.go`。

依赖：固定FreeSWITCH及真实业务ESL客户端。

验收方法：逐命令检查字节、头、编码、输出格式、事件顺序、重连和背压；Outbound需真实会话控制，不能用固定SIP上游替代。

完成条件：声明支持的命令/事件在实际客户端无改动通过，尚未覆盖的保留缺口。

对照ID：`key-esl-api`, `key-esl-bgapi`, `key-esl-framing`, `key-esl-outbound`, `key-esl-sendmsg`, `key-esl-subscriptions`, `key-cli-fs-cli`；验收/目标索引：`API-T01`。

### FS-02 · P1 · 882条配置导出的真实执行映射

**状态：**仅配置导出。**验证：**不构成运行验证。

882=875条vanilla参数+7条关键映射；配置可读写/导出不代表运行时加载、默认值和模块行为已兼容。

代码入口：`control/internal/server/fs_templates`；`control/internal/server/fs_config.go`。

依赖：模块优先级、配置生效/重启/回滚合同。

验收方法：逐项用真实配置加载并触发行为，检查默认值、错误、include、热加载/重启和回滚。

完成条件：只对实际支持的运行参数提升状态，模板与目录类条目保留导出范围，不批量改绿。

对照ID：`key-config-directory`, `key-config-export`, `key-config-parameter-edit`, `key-config-raw-xml`, `key-config-runtime-xml`。

### FS-03 · P2 · C/C++模块宿主与协议插件ABI

**状态：**部分实现/预留。**验证：**自定义示例有限通过，FS ABI未验证。

已有自定义codec ABI和G.711示例；无完整switch_*宿主符号，不能直接加载原版mod_*.so；协议provider未实现。

代码入口：`native/include`；`media/src/codec.rs`。

依赖：是否需要原版二进制插件；ABI版本、许可、故障隔离策略。

验收方法：用实际插件测试装载、卸载、生命周期、并发、异常和版本匹配，明确二进制/源码适配边界。

完成条件：所承诺插件实际运行；自定义C ABI不能被宣传为FreeSWITCH原生模块ABI。

对照ID：`key-c_abi-codec`, `key-c_abi-protocol-provider`。

### FS-04 · P2 · 会议及复杂PBX、脚本、传真和视频模块

**状态：**未实现/仅配置导出。**验证：**未验证。

目录内144个fs_module条目均为未实现，另有90条会议子命令；会议、队列/语音信箱、Lua/JS、传真、视频等不能从模板推断已具备。

代码入口：`control/internal/server/fs_templates`；`media/src/media/processed.rs`。

依赖：按Voice Agent业务选择外部复用或后续实现；保留全部差异。

验收方法：按原模块场景逐项验证音视频/混音/控制/故障和容量；不以主路径单元测试替代模块行为。

完成条件：首期可延期，但公开能力表仍列未实现；只对实际完成并有证据的子模块更新状态。

对照ID：`key-media-conference`。

### FS-05 · P1 · 3999条对照与177验收定义逐项证据

**状态：**未完成整体验证。**验证：**成对验证未运行，绝大多数条目未运行。

3999含原生声明、接口、模板和验收定义，不是3999个独立模块；177=169场景+8容量profile且均not_run。现有局部原版实验不代表全部原版行为执行。

代码入口：`tools/build_runtime_verification.py`；`docs/freeswitch-compatibility`；`docs/api/comparison.json`。

依赖：固定原版版本/提交、动态模块注册和原始用例；DLV-03。

验收方法：实例化每项场景，绑定双方源码、构建配置、二进制、输入/输出、失败及资源；以全部原始行导出追踪。

完成条件：不缩分母、不从实现状态算兼容百分比；缺原始轨迹或源码失配不判通过，失败/未运行可查。

验收/目标索引：`API-T01`。

### OPS-01 · P1 · NUMA、系统调用、队列和最忙worker观测

**状态：**部分实现。**验证：**完整运行成本与采样边界未验证。

已有部分资源指标和ASR计数；完整队列年龄、真实socket缓冲、同窗口CPU/内存、最忙分片和NUMA策略仍需完善。

代码入口：`control/internal/server/asr_observe.go`；`control/internal/server/server.go`；`media/src/media/worker.rs`。

依赖：DIA-01；Linux实际部署拓扑。

验收方法：同profile采集前后CPU增量、火焰图、IRQ/RSS、内存和队列年龄，优化前后对比并保持真实负载。

完成条件：指标口径与生成时间可解释；累计CPU不冒称媒体窗口CPU率，各进程独立峰值之和不冒称同一时刻峰值。

对照ID：`key-http-metrics`, `key-http-status`；验收/目标索引：`M3`, `A16`, `A17`, `V1-16`。

### OPS-02 · P0 · 灰度、回退与活动通话故障边界

**状态：**部分实现。**验证：**生产灰度/完整故障恢复未验证。

worker监督可结束受影响通话、继续其他分片和恢复新服务；不是活动通话或ESL TCP无缝迁移。主源码、候选和运行服务是三个状态。

代码入口：`control/internal/media/pool.go`；`control/internal/server/server.go`。

依赖：DLV-01、DLV-03、AI-08、CAP-05；明确发布授权及窗口。

验收方法：按1%→5%→20%→50%→100%灰度核对质量、容量与错误，验证新呼叫回旧服务、旧通话排空、配置回滚和进程故障。

完成条件：每档达到退出条件再推进，回退有实测收据；未实现的活动通话迁移不得宣称无损切换。

对照ID：`key-media-failover`, `key-guard-drain-resume`；验收/目标索引：`M4`, `A17`。

## 五、统一关单规则

每项“完成”至少绑定精确源码与依赖集合、实际二进制、原始输入/输出、样本和时刻、正常及故障分支、资源释放、测试退出结果。证据来源或期限失效时重新验证或降低状态，不能只改passed/绿色、删除失败或延长时间。测试事件数含子项，不是独立能力数。

容量分别运行RTP转发、真实音频处理、完整Agent三种profile，按1000→5000→10000逐档验收。FreeSWITCH比较必须同服务器、音频、质量、并发、时长与SLA，计入所有必需组件。远程模型成本可单列，本地模型必须计入；进阶目标不能从微基准推算。72小时混合负载和灰度/回退是独立退出条件。

本清单保留40个工作包编号用于分工和验收（含已完成的DLV-04）；这不是3999条对照的替代品或兼容率分母。完整逐行清单保留原ID、实现状态、本地状态、原因和差异；后续每次更新应同时维护接口文档、对照表和机器证据。

## 六、历史来源与校验记录

下表记录初次清单编制时的原工作区路径及哈希，用于历史追溯；这些路径不是公开仓库内的构建路径。当前文件位置见[交付与源码说明](11-交付与源码说明.md)，当前文件完整性见 `manifest.json`。

| 历史来源（原工作区相对路径） | SHA-256 |
|---|---|
| `outputs/rustswitch/docs/api/comparison.json` | `507e7cd6f0325a432630574f8321b7baaec351dd2dda4fee45732285214a376e` |
| `outputs/rustswitch/docs/api/runtime-verification.json` | `7416935c18a70665ab7be6fd9a878a06c737fb8fab1329f6f82631770aba45f5` |
| `outputs/rustswitch/docs/api/compatibility-worklist.md` | `5d46a2f19a497825a65a11a30418c773d2df125662285f227a9ed01a4bf3459a` |
| `outputs/rustswitch/docs/api/voice-runtime-v1-acceptance.md` | `a96ea6ec025bc7e98ad2d7fd3a8a22f0a6b318183cf01eb7ba0c5d4cbe22c8e2` |
| `work/voice-runtime-m2-asr/CHECKPOINT.md` | `4ad8337bdfc730740a82e93594603d09494237c7d2601c9171ac481a42dad2ed` |
| `work/voice-runtime-m2-asr/full-review-01/receipt.json` | `961f33471bc34b00473ab1ff2394b6a81dca005f1c7ec8ab327cd0845f44e781` |
| `work/voice-runtime-m2-asr/benchmark-fix-01/baseline.json` | `fa784c08a342a2bfdfc38bcc107f5374b3adbbcb79a768b6f09a98866ee05a53` |
| `work/voice-runtime-m2-asr/benchmark-fix-01/test-02/receipt.json` | `96fecc4cd3b1fa442edad934a05d301b9039928f4e8d4dd7f4fad42eb3aa9799` |
| `work/voice-runtime-m2-asr/benchmark-fix-01/unified.patch` | `0f369c2b5f34e9c0316aa4ad8c1c53fa7e909ef51bcd09012bbbed059118d8fd` |
| `work/project-delivery-2026-09-08/patch-state.json` | `d472fa77d73e7212072d036b860856f62eb8bcd91dd9639217e42eafe8205838` |
| `work/project-delivery-2026-09-08/build-main-01/receipt.json` | `02d4846ce28e02d835b3bf785b1499f74c08c59383d1e2a6d826d8df2ba988df` |
| `work/project-delivery-2026-09-08/build-asr-candidate-01/receipt.json` | `a10677f9d4577a831706f27a16cdd25aef3c20c3f392c0965d9a3e3f843fc4f1` |

ASR候选当前相对full-review-01所绑定文件的内容变化数量：0。此处仅比较该收据的明确文件范围，不将范围外新增源码自动判为已测。已公开 benchmark 工作稿相对 test-02 的变化：`control/internal/server/benchmark_failure_diagnostics_test.go`, `control/internal/server/benchmark_runner.go`；两文件未复验。历史机器摘要见 [backlog-summary.json](backlog/backlog-summary.json)。

