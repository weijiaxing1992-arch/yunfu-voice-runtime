# 11 交付与源码说明

RustSwitch 的公开交付包含主工程、独立 ASR 候选、测试与工作补丁、固定依赖、完整项目资料和两个 ARM64 平台的预编译程序。本章说明如何选择源码、构建产物和验证依据。

首个发行版本 [v0.1.0-preview.20260908](https://github.com/weijiaxing1992-arch/yunfu-voice-runtime/releases/tag/v0.1.0-preview.20260908) 已公开，绑定提交 `54fcd571874d599b10611dc7f38f2a751a433c94`。该发行的 4,991 个 Git 文件及 8 个附件已完成上传核对。后续 main 分支的文档更新单独通过 Git 记录，不替换已发布附件或重写历史证据。

## 获取与选择

| 需求 | 入口 | 内容 |
| --- | --- | --- |
| 浏览或参与开发 | [GitHub 仓库](https://github.com/weijiaxing1992-arch/yunfu-voice-runtime) | 完整源材料、文档、模板与运行包说明；可执行文件通过 Releases 分发 |
| 固定版本复现 | [发行源码树](https://github.com/weijiaxing1992-arch/yunfu-voice-runtime/tree/v0.1.0-preview.20260908) | 对应首个预览版提交，避免 main 后续变更影响复现 |
| 一次下载全部材料 | Release 中 `yunfu-voice-runtime-complete-materials-20260908.zip` | 源码、依赖、资料及两个平台的运行包；解压后打开 `index.html` |
| 仅取完整源码 | Release 中 `yunfu-voice-runtime-source-20260908.zip` | 主工程、候选、补丁、设计参考、构建依赖和交付工具 |
| 直接体验程序 | Release 中 macOS ARM64 或 Linux ARM64 ZIP | 按目标系统选择；先读平台说明并检查配置 |

下载文件名保留 `yunfu-voice-runtime`，以保证现有链接、来源记录和校验值稳定；产品公共名称为 **RustSwitch**。

## 材料目录

```text
repository/ 或完整资料包根目录
├── README.md、DELIVERY.md、index.html    社区首页与资料入口
├── 01-…13-*.md / *.html                项目、架构、接口、使用、需求和缺口
├── source/
│   ├── main/                           主工程完整源码
│   ├── asr-candidate/                  独立 ASR 候选完整源码
│   ├── patches/benchmark-tested-02/     已完成定向测试的诊断补丁
│   ├── patches/benchmark-working/       含未复验改动的后续工作稿
│   ├── design-reference/               ASR 协议夹具与离线设计
│   └── *manifest.json、asr-vs-main.*    源码校验与候选差异
├── dependencies/                       固定 Rust vendor 和 Opus 源码
├── delivery-tools/                     可搬移的离线构建工具
├── binaries/                           平台说明、配置、来源与许可
├── backlog/、requirements/             全量差异及需求追踪
├── evidence/、candidate/               历史收据及候选说明
├── figures/、reference/                图源、接口合同与专题原文
└── open-source/、runtime-notices/       公开处理记录与许可补充
```

Git 仓库的 `binaries/` 不携带 `bin/` 和 `lib/` 中的可执行文件或动态库；这些文件位于对应 Release 运行包及完整资料 ZIP 中。GitHub 自动生成的源码归档也不等于包含运行程序的完整资料 ZIP。

## 源码版本与完成范围

以下文件数和差异统计固定于首个公开预览版。

| 内容 | 规模与用途 | 已记录验证及限制 |
| --- | --- | --- |
| [主工程](source/main/README.md) | 1,126 个文件，含 Go/Rust/C、后台、配置、部署、测试、vendor 与内嵌文档 | 公开副本离线构建通过；文档基线 1.13.0；文档门禁仍有阻塞 |
| [ASR 候选](source/asr-candidate/README.md) | 1,171 个文件，含 ASR1、mock、重采样、期限、配额回收和观测实现 | 离线构建通过；独立候选未合并主工程；mock 不运行识别模型 |
| [已测诊断补丁](source/patches/benchmark-tested-02/files.json) | 6 个改动文件及冻结 `unified.patch` | test-02 定向 unit/race 与页面函数测试通过；未合并，仍依赖原子统计问题的后续修复 |
| [后续工作稿](source/patches/benchmark-working/files.json) | 相对原基线的完整补丁与 6 个改动文件 | runner 和测试两个文件较 test-02 更新；尚无 test-03 完整复验结论 |
| [独立 ASR 测试设计](source/design-reference/asr1-fixture/README.md) | 设计文档、离线夹具与验收矩阵 | selftest-03 保留 52 个离线方法的历史证据；不等于生产互通或完整 Agent 验收 |

候选与主工程共 59 个差异路径：新增 47、修改 10、未带入 2。未带入项为主工程 `.gitignore` 与 systemd 模板；候选保留自己的实际目录状态。二进制测试向量以完整候选目录及 JSON 清单为准，`asr-vs-main.text.patch` 只用于文本审查，无法单独重建所有向量。

[历史绑定报告](source/historical-test-binding.json)记录原 ASR `full-review-01` 收据覆盖的 896 个文件在打包时的核对结果。原验证含 504 个顶层、1,471 个含子项测试事件、vet、页面函数和双平台编译，不覆盖收据以外的源文档、Makefile 增量及未复验压测工作稿。

## 已交付的主要开发增量

| 代码位置（相对候选源码树） | 内容 |
| --- | --- |
| `control/internal/media/rx_*` | RX 元数据、观察时钟、剩余期限、过期与暂停边界 |
| `control/internal/asr/` | ASR wire、流读写、来源验证、有界队列、FINISH/DONE 与取消 |
| `control/internal/asr/resample/` | 固定系数、独立参考向量、8→16 kHz 及重采样状态边界 |
| `control/internal/server/asr_stream*`、`rx_stream*` | ACK 资格、本地通道、生命周期撤权与无绑定配额回收 |
| `control/cmd/asr-mock/` | Unix 协议模拟代理、PCM 摘要与故障注入 |
| `control/internal/config/asr_stream*`、Server 观测代码 | ASR 配置、状态、指标和总览观测 |
| `source/patches/`（仓库根目录） | 压测失败后有界诊断、保留原失败及 socket 丢包支持能力显示 |

原创代码保留既有中文注释；第三方源码、固定依赖和历史证据保留原文及来源，不通过翻译或格式重写改变上游身份。

## 离线构建

需要 Python 3.9+、Go 1.23+、Rust/Cargo 1.85+ 和支持 C11 的本机 C 编译器。macOS 还需要开发工具与系统 SDK；工具链本身不随源码包分发。最低版本来自源码声明和脚本用法，已记录的构建使用 Go 1.27.1、Rust 1.98.1，并未逐个最低版本重建。

从仓库或完整源码包根目录执行：

```sh
python3 delivery-tools/build_source.py \
  --variant main \
  --output ../rustswitch-build-main

python3 delivery-tools/build_source.py \
  --variant asr-candidate \
  --output ../rustswitch-build-asr
```

输出目录必须尚不存在且位于资料目录之外。脚本核对源码清单，使用 Go `-mod=vendor`、Cargo `--locked --offline` 与随包依赖，生成控制程序、callbench、Rust 媒体和 G.711 C ABI 示例；候选另生成 asr-mock。可通过 `GO`、`CARGO`、`CC` 指定工具位置。结果与各步骤日志保存在输出目录，查看 `receipt.json` 的 `passed` 和 `source_unchanged`。

基础构建**复用现有内嵌文档**，不重新生成后台资料，不运行 SIP、容量或完整功能验收。G.722/Opus 为额外原生后端，按[原生音频说明](source/main/native/audio/README.md)单独构建。完整启动、路径组装和配置检查步骤见 [快速开始](QUICKSTART.md)。

原始工程的 `make all` 会进入文档构建步骤；当前两份源码的文档门禁仍有记录在案的失败，详见 [当前能力与验证状态](08-当前能力与验证状态.md)。开发构建通过不应被描述为完整发布验收通过。

## 预编译程序与验证材料

| 平台 | 包含内容 | 使用范围 |
| --- | --- | --- |
| [macOS ARM64](binaries/macos-arm64/README.md) | 主工程、ASR 候选及可选原生动态库 | 完整包按 macOS 26+／Apple Silicon 使用；开发构建未做 Developer ID 公证 |
| [Linux ARM64](binaries/linux-arm64/README.md) | 主工程及 ASR 候选静态核心程序 | aarch64；不含可选 G.722/Opus `.so`；候选组合尚未在 Linux 完成整组联调 |

当前没有 Linux x86_64 预编译包。预编译附件保留 20 个原构建文件的字节与身份；公开副本的文本整理不会同步改写可执行文件里的历史文档。Linux 主工程保留的限定探针记录不构成容量验收。

发行附件 `public-build-checks.json` 记录公开副本的离线构建和配置检查；`publication-validation.json` 记录分发内容与链接校验。原始开发构建收据保留于 [main](evidence/delivery-build/main/receipt.json) 和 [ASR 候选](evidence/delivery-build/asr-candidate/receipt.json)。这些文件分别描述自己的执行范围，不能互相替代。

## 完整性、许可与后续修改

下载时使用同一发行中的 `SHA256SUMS.txt` 核对附件，再按运行包 `verify.py` 核对解压内容。`source/*-manifest.json`、整包 `manifest.json` 和运行包清单分别对应自己的材料范围；源码发生修改后，旧快照清单不再是新源码的身份声明。

自有原创代码和资料采用 Apache-2.0。G.722 支持文件、SpanDSP、FreeSWITCH 模板及其他依赖保留各自许可，详见 [开源说明](OPEN_SOURCE.md)、[许可范围](LICENSE_SCOPE.md)和[第三方清单](THIRD_PARTY_NOTICES.md)。实际运行密钥、服务状态、socket 和开发缓存不在源码交付范围内。

后续贡献应通过新的提交记录变更，以新证据更新适用功能状态；发行附件和历史收据保持冻结。完整任务边界见 [需求说明书](12-需求说明书.md)与[未完成清单](13-项目未完成清单.md)。
