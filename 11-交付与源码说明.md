# 11 交付与源码说明

本次将完整源码、已完成候选、尚未复验工作稿、需求与未完成清单加入原来的同一个资料目录。入口仍是 `index.html`。**源码编译通过，发布门禁仍有明确阻塞**；版本身份与验证范围见下表。

## 交付目录

```text
云蝠VoiceRuntime-项目资料-2026-09-08/
  index.html、01…10                 上轮完整架构、接口、使用与运维资料
  11-交付与源码说明.md / .html        本说明
  12-需求说明书.md / .html            功能、性能、边界、阶段与验收要求
  13-项目未完成清单.md / .html        逐项缺口、优先级、依赖和完成条件
  source/main/                      主工程完整源码快照
  source/asr-candidate/             已完成ASR开发的完整候选源码快照
  source/patches/benchmark-tested-02/ 已通过定向检查的压测诊断补丁
  source/patches/benchmark-working/   后续工作稿，包含未复验改动
  source/design-reference/          已完成的独立ASR协议离线测试设计
  source/*manifest.json              源码逐文件SHA-256清单
  source/asr-vs-main.*                候选与主工程差异
  dependencies/rust-vendor/          Cargo.lock固定Rust依赖源码
  dependencies/opus-1.5.2.tar.gz      可选编解码后端固定上游源码
  delivery-tools/build_source.py     可搬移的离线编译工具
  backlog/                          全部3999条FS对照及分状态清单
  requirements/                     用户方向原文与需求追踪资料
  evidence/                         编译、发布门禁及候选历史验证原件
  figures/、reference/、candidate/   上轮图源、专题及候选说明
  manifest.json、validation.json     整包完整性与资料校验记录
```

## 哪套代码用于哪项工作

| 交付内容 | 范围 | 实际验证 / 当前边界 |
|---|---|---|
| [主工程](source/main/README.md) | 1126 个文件，Go/Rust/C、后台、配置、部署、测试、工具、vendor、文档与内嵌资源 | 本轮离线编译通过。文档基线1.13，组件版本另计；本轮未部署 |
| [ASR候选](source/asr-candidate/README.md) | 1171 个文件，包含主业务基础及ASR1、mock、重采样、来源证明、消费时效、取消/配额清理、总览观测及测试 | 本轮离线编译通过，包含asr-mock。候选尚未合并发布，自己的部署目录本来不存在 |
| [已测诊断补丁](source/patches/benchmark-tested-02/files.json) | 6个改动文件及冻结unified.patch；从原基线还原后逐个核对哈希 | test-02定向unit/race、页面函数测试通过。使用旧观测新鲜度判断，仍依赖待修的原子统计问题，未合并 |
| [压测后续工作稿](source/patches/benchmark-working/files.json) | 相对原基线的完整补丁与6个改动文件；其中runner及测试2文件较test-02更新 | 新终态helper和用例尚未完成test-03；没有新的完整验证结论 |
| [独立ASR测试设计](source/design-reference/asr1-fixture/README.md) | 六份设计、离线协议夹具及验收矩阵 | selftest-03的52个离线方法有历史证据；不作为生产协议互通或完整Agent验收 |

主/候选差异共59个路径：新增47、修改10、未带入2（主工程 `.gitignore` 与 systemd 模板）。完整目录保留各自状态，没有把候选拷回主工程。二进制测试向量在完整候选目录中；`asr-vs-main.text.patch` 仅用于文本审查，**不能单靠这份文本补丁重建二进制向量**，重建以完整目录及JSON清单为准。

原 ASR `full-review-01` 收据绑定的896个文件与本次候选逐个哈希吻合。原结果是504个顶层、1471个含子项测试事件，以及vet、页面函数检查和双平台编译。此绑定不包括未列入原收据的Makefile/源文档增量，更不覆盖压测工作稿。见 [绑定报告](source/historical-test-binding.json)。

## 已完成代码的具体增量

1. RX元数据、原始观察时钟与剩余期限：`control/internal/media/rx_*`，覆盖过期、丢弃、暂停及消费边界。
2. ASR协议与客户端：`control/internal/asr`，分离wire、流读写、帧与来源验证、有界队列、FINISH/DONE和取消。
3. 按需重采样：`control/internal/asr/resample`，固定系数、独立参考向量、8→16k及状态边界。
4. 真实本地通道接入：`control/internal/server/asr_stream*` 与 `rx_stream*`，正确ACK授权、生命周期撤权及无绑定配额回收。
5. 可控代理：`control/cmd/asr-mock`，真实Unix连接、PCM摘要及故障注入；不含真正识别模型。
6. ASR配置和观测：`control/internal/config/asr_stream*`、Server观测、HTTP状态/指标及总览代码。
7. 压测诊断：失败后有界采样、保留原失败、socket丢包支持能力显示；已测与未测版本分别交付。

源码文件保留开发时的中文注释。Go vendor、Rust vendor、上游C源码和历史证据按原始内容保存；不把第三方注释翻译后冒充原件。

## 离线编译步骤

在交付目录执行，系统需安装 Go≥1.23、兼容工程的Rust stable（最低1.85）、C编译器和Python3。工具链本身未随包提供。以下命令编译开发产物，不启动电话服务：

```sh
python3 delivery-tools/build_source.py --variant main --output /tmp/voice-main-build-01
python3 delivery-tools/build_source.py --variant asr-candidate --output /tmp/voice-asr-build-01
```

输出目录必须尚不存在且位于交付目录之外。脚本用 `-mod=vendor` 编译Go，用 `--locked --offline` 和本包Rust依赖编译媒体，并生成G.711 C ABI示例。可通过环境变量 `GO`、`CARGO`、`CC` 指定工具位置。两套源码均已按此脚本在本机实际执行成功，原始日志和收据见 [主工程编译](evidence/delivery-build/main/receipt.json)、[候选编译](evidence/delivery-build/asr-candidate/receipt.json)。

源码中的原始 `make all` 仍执行文档发布门禁。开发编译成功不等于该门禁通过；脚本不会更新、跳过后宣称通过或篡改绿色证据。Linux ARM64历史编译也不等于Linux运行或容量验收。

Go第三方依赖已在各源码树 `control/vendor` 中，旧README里的“无第三方Go模块”描述已过时，实际为固定 `golang.org/x/sys v0.30.0`。可选Opus原始tar包已按工程 `native/audio/sources.json` 的SHA核对；原生后端的实际构建参数、加载路径和许可见原专题，基础编译不自动开启全部编码实时处理。

需要试运行时，在自己的运行目录复制 `source/main/config/local.json`，把 `media.binary` 指向上述构建输出里的 `cargo-target/release/rustswitch-media`；先用同一输出目录的 `rustswitch -check-config -config <配置路径>` 校验，再按06章配置端口、线路与日志目录。源码快照中不补入运行配置或构建产物。

## 发布前仍需完成的检查

本轮实际执行两份源码的 `python3 -B tools/build_api_docs.py --check`：

| 源码 | 结果 | 后续处理 |
|---|---|---|
| main | 失败：`绿色已失效：key-api-pcm-stream` | 按冻结源码重新取得有效证据并更新派生文档；不能只改时间戳或绿色字段 |
| asr-candidate | 失败：`字段字典落后于机器合同` | ASR源OpenAPI增量需同步字段字典、内嵌资源与当前源码验证；首个错误修复后继续跑完整门禁 |

[原始门禁收据](evidence/delivery-doc-gate/receipt.json)保留失败。原对照中的82项local passed是交付参考文件的存量统计，不能全量当作本日仍有效的绿色认证。当前没有新发布、万路、完整供应商Agent或100% FreeSWITCH通过结论。

## 交接时应核对什么

- `source/main-manifest.json`、`source/asr-candidate-manifest.json` 与整包 `manifest.json` 能逐文件校验。
- `source/packaging-snapshot.json` 记录原始来源、排除项、差异和工作稿边界。
- [独立源码审计](evidence/source-audit/source-audit.md)记录副本一致性、早期实现纳入情况、未复验改动与证据范围。
- `backlog` 保留全部原ID与状态，可按需求编号和模块逐项分派，不能只按绿数推进。
- 运行密钥、主服务状态、socket、构建缓存和旧安装二进制未作为源码打包。验收测试向量、必要配置示例、完整vendor和第三方许可已经保留。
- 本次公开发布已提供根级 Apache-2.0 LICENSE；自有代码与原创资料适用该许可，第三方组件保留原许可及例外，详见 [开源说明](OPEN_SOURCE.md)。
- 本轮以源码及材料交付为范围，未重启或替换正在运行的服务。
