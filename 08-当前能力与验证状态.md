# 08 当前能力与验证状态

本章说明 **RustSwitch（云蝠 Voice Runtime）** 的实现范围、验证依据和剩余工作，供技术选型、集成评估与验收使用。公开版本为开发预览，源码分发已经完成；业务功能、兼容性和容量分别按对应证据判断。

## 版本与证据基线

| 对象 | 固定标识 | 适用范围 |
| --- | --- | --- |
| 首个公开预览版 | [v0.1.0-preview.20260908](https://github.com/weijiaxing1992-arch/yunfu-voice-runtime/releases/tag/v0.1.0-preview.20260908) | 2026-09-08 发布的源码与 8 个发行附件；附件保持该次快照 |
| 发行源码提交 | `54fcd571874d599b10611dc7f38f2a751a433c94` | 4,991 个 Git 文件；这是发行时的文件数，不作为后续 main 分支的固定数量 |
| 主工程文档基线 | 1.13.0 | 主工程 HTTP、数据模型和对照目录；不是 FreeSWITCH 版本号 |
| 组件版本 | Go 控制程序 0.3.0；Rust 媒体 0.2.0 | 各组件独立版本，不与公开发行标签或文档版本直接比较 |
| FreeSWITCH 参考 | 1.11.3，提交 `ef32e205295e29f034f1453ad245ba5efb07b94a` | 固定对照来源，不表示完成原版运行验收 |
| ASR 实现 | `source/asr-candidate/` | 独立候选快照，尚未作为主工程功能合并 |

main 分支可继续完善文档；这些修改不会重写冻结发行附件、原始收据、二进制身份或历史测试结论。复现某次测试时，应同时固定源码、配置、平台、工具版本和证据文件。

## 如何理解状态

| 状态 | 可以得出的结论 | 不能据此推导的结论 |
| --- | --- | --- |
| 已实现 | 对应代码、入口或配置处理存在 | 所有正常、异常和互通场景均通过 |
| 有限验证通过 | 指定快照在记录的平台与用例下通过 | 后续修改、其他平台或完整产品通过 |
| 仅配置导出 | 可以编辑或生成配置文件 | 运行时已消费配置并具备对应业务能力 |
| 未实现／部分实现 | 尚缺入口、行为或场景 | 可无感替换原版同名功能 |
| 未验证／证据失效 | 缺少适用的执行证据，或证据与源码不再匹配 | 可凭历史绿色状态继续验收 |

构建通过、配置检查通过、压缩包完整性通过、业务验收通过是不同结论。状态展示应保留这种区分，不用单一百分比代替实际适用范围。

## 当前工程能力

| 方向 | 已提供的工程内容 | 集成边界与下一步 |
| --- | --- | --- |
| 电话与控制 | SIP 呼叫处理、线路配置与注册相关实现、呼叫准入和管理后台 | 按目标线路验证身份映射、注册周期、完整 originate、转接及 SIP 扩展 |
| 核心媒体 | Rust 媒体进程、RTP/RTCP 处理、G.711 PCMA/PCMU、PCM 收发相关实现 | 按真实网络验证丢包、抖动、超时、资源回收和发送尾延迟 |
| 交互与迁移 | DTMF、播放与收号相关实现，部分 ESL、IVR 与配置对照 | 逐项核对原版语法、事件顺序和错误语义；同名入口不自动等价 |
| AI 接入候选 | ASR1、Unix 客户端、PCM 来源与期限检查、8→16 kHz 重采样、模拟代理 | 尚未形成真实 ASR/TTS 供应商和完整 Voice Agent 链路 |
| 管理与测试 | HTTP 管理接口、可视化配置、保护参数、隔离 SIP/媒体测试和压力发生器 | 小电话是单路模拟测试；页面并发档位不代表已取得相应容量认证 |
| 原生音频扩展 | C ABI、G.722/Opus 可选后端和相关探针 | 完整实时音频图、音质及跨平台装载仍需专项验证 |

具体字段与调用方式见 [HTTP 接口](04-HTTP接口文档.md)、[协议与 SDK](05-协议与SDK文档.md)；逐条差异见 [未完成清单](13-项目未完成清单.md)。

## FreeSWITCH 对照目录快照

1.13 文档基线保存 **3,999 条**声明、接口、配置和验收记录。下表是冻结目录的实现分类，每条记录不等于一个独立业务功能。

| 实现分类 | 条目数 | 解释 |
| --- | ---: | --- |
| 已实现自有入口 | 19 | RustSwitch 自有入口已存在；另查原版映射与语义差异 |
| 内部接口 | 23 | 自有内部调用或媒体合同 |
| 部分实现 | 53 | 已列明尚缺行为或场景 |
| 仅导出配置 | 882 | 可编辑或导出，运行消费另行核验 |
| 未实现 | 2,844 | 对应入口或业务尚未完成 |
| 未验证 | 178 | 相应完整验收合同等尚未完成 |

同一基线的本地执行账本记录 **82 条有限通过、3,916 条未运行、1 条失败**。实现分类与执行账本是两个统计维度；不应相加，也不构成完整 FreeSWITCH 兼容率。82 条历史通过不是当前仍有效的 82 个模块认证，且不会自动继承到 ASR 或压测候选。

## 已记录的 ASR 候选验证

以下结果来自保留的历史收据。本次文档整理没有重新执行这些测试，也没有扩大原断言。

| 验证 | 记录结果 | 证据及适用范围 |
| --- | --- | --- |
| Go 全包竞态回归 | 1,471 个通过测试事件，其中 504 个顶层测试；无失败或测试用例跳过 | [full-review-01](evidence/asr/full-review-01/receipt.json)；两个无测试包不计为业务用例通过 |
| 静态与构建检查 | vet、页面函数检查及 Darwin/Linux ARM64 编译通过 | 同一收据；Linux 编译不等于 Linux 运行或性能验收 |
| 独立 Unix 代理 | 9 个场景及独立审计通过 | [客户端审计](evidence/asr/client-independent-audit-02/receipt.json)；输入为测试 Source，代理不运行识别模型 |
| SIP／Rust／ASR 接入 | 5 个场景通过 | [server-real-02](evidence/asr/server-real-02/receipt.json)；PCMU/PCMA、8/16 kHz、晚订阅、ACK 资格、FINISH/DONE、正常 BYE 与清理 |
| 独立样本审计 | 44 个 RTP 输入含 4 个预订阅包；40 帧进入 ASR；8,960 个输出样本 | [服务端独立审计](evidence/asr/server-independent-audit-01/receipt.json)；核对 RTP 展开、G.711 解码、重采样、期限与资源 |
| 配额回收修复 | 定向 race 98 项与 vet 通过 | [无绑定回收收据](evidence/asr/server-no-binding-fix-01/receipt.json)；仅实际确认无 RX 绑定或退休后释放未知槽 |

5 个真实 SIP 场景的 BYE 均发生在正常 Finish 后；晚订阅不回放 kind 7，但首个 Decoded 仍有 Boundary 5。这些结果不覆盖所有“挂断／打断／等待 final”组合。首轮因测试 oracle 漏算 Rust 初始展开周期而失败的记录保留，修正后另存重跑收据。

[历史绑定报告](source/historical-test-binding.json)记录原收据覆盖的 896 个文件在打包时的核对结果。该报告不覆盖收据以外的 Makefile、源文档增量、后续公开处理或未复验工作稿；公开副本的哈希清单用于另外核验分发字节。

## 尚未通过的文档门禁

2026-09-08 保留的两份源码检查均未通过，见 [原始门禁收据](evidence/delivery-doc-gate/receipt.json)。这不妨碍按开发预览分发源码，但必须在后续功能验收中解决。

| 源码 | 首个记录阻塞 | 完成条件 |
| --- | --- | --- |
| 主工程 | `绿色已失效：key-api-pcm-stream` | 针对对应源码重新执行所需用例，核对证据绑定并更新派生文档 |
| ASR 候选 | `字段字典落后于机器合同` | 同步 OpenAPI、字段字典、内嵌资源及有效验证依据，再运行完整门禁 |

这是各次检查遇到的首个错误，不表示只剩两项问题。不能通过更改时间戳、历史哈希或绿色字段将失败变成通过。

## 后续验收重点

| 优先方向 | 需要完成的工作 |
| --- | --- |
| AI 主链路 | 真实 ASR/TTS 适配、LLM/Turn 编排、VAD 与打断、双轨录音、双工故障及结果时效 |
| 核心音频 | G.722/Opus 实时处理、PLC/FEC 等必要机制，以及同音质条件下的对比 |
| 电话适配 | 目标运营商线路、注册与身份映射、呼叫与转接、SIP 扩展、必要的终端行为 |
| 稳定与容量 | 原子一致观测、失败诊断、Linux 1,000/5,000/10,000 路、长时间稳定性、最忙分片与尾延迟 |
| 部署与恢复 | 灰度、排空、回滚、故障恢复；活动通话跨机无损迁移尚无验收结论 |
| 按需求评估 | 通用 PBX、会议、传真、视频、任意脚本与模块宿主；延后项仍保留在原版差异目录 |

阶段目标与通过条件以 [V1 长期验收合同](reference/docs/api/voice-runtime-v1-acceptance.md)、[需求说明书](12-需求说明书.md)和[未完成清单](13-项目未完成清单.md)为准。当前没有完整 FreeSWITCH 兼容、5,000/10,000 路生产容量、完整真实供应商 Agent 或任意故障零丢包的证明。
