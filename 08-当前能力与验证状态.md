# 08 当前能力与验证状态

> 交付补充：2026-09-08 实际源码发布门禁发现主工程 `key-api-pcm-stream` 绿色证据已失效，ASR候选字段字典待同步。下述82个本地passed为原始状态快照，不是本日全部仍有效的认证。见 [交付说明](11-交付与源码说明.md)。

## 工程基线的对照计数

固定 1.13 文档基线保存 **3999 条对照**。实现分类如下；每条是目录/契约记录，不等于独立业务功能。

| 状态 | 条目数 | 含义 |
|---|---:|---|
| 已实现自有入口 | 19 | 不自动等于原版同协议/同语义 |
| 内部接口 | 23 | 自有内部调用或媒体合同 |
| 部分实现 | 53 | 仍有明确差异 |
| 仅导出配置 | 882 | 可编辑/导出，不代表运行消费 |
| 未实现 | 2844 | 对应入口/业务未完成 |
| 未验证 | 178 | 原完整验收合同等仍未完成 |

该冻结基线的本地运行账本为 **82 条有限通过、3916 条未运行、1 条失败**。这是 1.13 自身证据，不自动继承到后续 ASR/统计候选，也不是 82 个原版模块认证。原始分类、原版完整契约和本地运行结果分别展示。

## 本轮 ASR 候选的已执行验证

| 验证 | 实际结果 | 范围 |
|---|---|---|
| 全 Go 包竞态回归 full-review-01 | 1471 个通过测试事件，其中 504 个顶层；无失败或测试跳过 | 实际本机 Go 控制/协议/媒体/ASR 集合；两个无测试包不是跳过功能用例 |
| 静态/构建 | vet 通过；Darwin、Linux ARM64 编译通过 | Linux编译不是Linux运行/性能验收 |
| 独立 Unix 代理 | 9 场景，通过并独立审计 | RX输入为测试Source；验证真实Unix字节和故障，代理不运行模型 |
| 真 SIP/Rust/ASR | 五场景通过 | PCMU/PCMA×8/16k与晚订阅，真实ACK资格、40帧逐样本、FINISH/DONE、正常BYE与清理 |
| 独立样本审计 | 44个RTP输入含预订阅4包；40帧进入ASR；8960输出样本 | 完整RTP展开、G.711解码、16k独立卷积、消费期限和资源均核对 |
| 配额回收修复 | 定向race 98项与vet通过 | 只有实际确认无RX绑定/退休才释放未知槽 |

来源见 candidate/evidence 下的原收据，文件哈希可追溯。五个真实 SIP 场景的 BYE 均在正常 Finish 后；晚订阅不回放 kind7，但首 Decoded 仍有 Boundary5。不得将其扩写成“所有挂断打断待返回final的组合已通过”。

第一轮 SIP 失败记录因测试 oracle 漏算 Rust 初始展开周期保留；修正后重新运行产生新收据，没有覆盖旧失败。独立 mock 是协议和PCM摘要工具，不代表识别准确率。

## 产品缺口与优先顺序

| 优先方向 | 剩余工作 |
|---|---|
| AI主链路 | 真实ASR/TTS厂商适配、LLM/Turn编排、VAD/打断、双轨录音、完整双工故障和结果时效 |
| 核心音频 | G.722/Opus实时图、必要语音增强/PLC/FEC及同音质证明；其他codec按真实线路需求 |
| 电话适配 | 完整originate/转接、SIP扩展、目标运营商身份映射、多网关/终端注册等 |
| 稳定与容量 | 原子一致观测、失败诊断、Linux 1000/5000/10000、长稳、最忙分片与发送尾延迟 |
| 生产交付 | 灰度、排空、回滚、故障恢复；现阶段没有活动通话跨机无损迁移 |
| 按需/延后 | 通用PBX、会议、传真、视频、任意脚本/模块宿主；延后项仍保留在原版分母 |

完整 V1 能力与 M1—M4 目标保持不变，见 [长期验收合同](reference/docs/api/voice-runtime-v1-acceptance.md)。本资料包没有修改原对照绿数，没有发布新后台，也没有为任意故障承诺零丢包。
